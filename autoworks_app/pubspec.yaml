name: autoworks_app
description: AutoWorks Bolivia — App móvil

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1

flutter:
  uses-material-design: true